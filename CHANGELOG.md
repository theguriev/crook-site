# Changelog

Written by `scripts/release` from the commits between one tag and the next,
and read back by `scripts/release-notes`. Nothing here is published on a tag: the
site goes out when somebody runs `scripts/deploy.sh`.
It starts at the first release cut that way, which is the first one there is.
